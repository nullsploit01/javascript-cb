const players = [
  "kobe",
  "messi",
  "ronaldo",
  "michael jordan",
  "stephen curry",
  "mike tyson",
];

// for (let i = 0; i < players.length; i += 1) {
//   console.log(players[i]);
// }

// const printPlayer = (player) => {
//   console.log(player);
// };

// players.forEach(printPlayer);

numbers = [1, 2, 3, 4, 25, 6, 7];

const square = (number) => {
  return number * number;
};

console.log(numbers.map(square));
